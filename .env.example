# Only needed when your backend is NOT on the same host (e.g. production or different port).
# Leave empty or omit for local dev (Vite proxies /api to http://localhost:5000).

# Backend API URL (no trailing slash). Examples:
# VITE_API_URL=http://localhost:5000
# VITE_API_URL=https://mk-trading-api.onrender.com

VITE_API_URL=
